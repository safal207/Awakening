# Проверка заготовки Unreal

Дата: 2026-09-10. Проверка выполнена в Windows, в `C:\Maincraft`.

| Проверка | Результат |
|---|---|
| Обычные каталоги Epic, LauncherInstalled.dat, реестр установок | Unreal и Launcher не найдены |
| Доступный C++ Build Tools | Найден `C:\BuildTools`; совместимость с UE не подтверждена |
| 10 тестов `unreal/tests/test_scaffold.py`, Python 3.12.14 | PASS |
| Синтаксический разбор `prepare.ps1` PowerShell Parser | PASS |
| `prepare.ps1 -CheckOnly` без движка | Ожидаемая понятная ошибка, без установки и запуска сборки |
| Release-сборка и `--functional-test` прежней .NET-игры | PASS |
| `git diff --check` | PASS; предупреждения LF/CRLF не являются ошибками |
| C++ Unreal / Unreal Header Tool | НЕ ЗАПУЩЕНЫ: движок отсутствует |
| Генерация `.uasset` / `.umap` через настоящий Unreal Python | НЕ ЗАПУЩЕНА |
| Play, физика, анимация, визуальная проверка | НЕ ЗАПУЩЕНЫ |
| FPS, GPU frame time, VRAM, память за 15 минут | НЕ ИЗМЕРЕНЫ |

Python-тесты проверяют данные плана и структуру, а не движок. Отсутствие
пересечений маршрута с расширенными прямоугольниками не доказывает корректность
физики Unreal. Проход .NET-тестов подтверждает лишь сохранность прежнего проекта.
Итог: подготовка к запуску, а не подтверждённая Unreal-сборка или реализм.
